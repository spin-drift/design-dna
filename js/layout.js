// Layout module — intentionally empty.
//
// This file was used during a series of experiments with automatic
// layout algorithms (dagre, then ELK, then a hybrid manual scheme).
// We reverted to the manual `BRANCHES[*].rows` system in data.js
// because it gave us the most direct control over the visual outcome,
// which matters for a study tool where the tree's shape is part of
// the editorial argument.
//
// Kept as a placeholder so future imports won't 404 if anything still
// references the path. If we ever want automatic layout back, the
// reference implementations are in the git history.
